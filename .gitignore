# Secrets & runtime data — never commit
.env
*.env

# User/runtime JSON
subscribers.json
quiz_scores.json
fun_duels.json
fun_daily.json
fun_stats.json
fun_badges.json
fun_quiz_cache.json

# Logs
*.log
logs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Media & generated files
*.mp3
*.wav
*.ogg
*.mp4
*.m4a
generated-icon.png
mp3_cache/
*.part

live_charts_cache.json

# Home-worker runtime state (live on the server, never commit)
worker_jobs.json
worker_heartbeat.json

# Share card runtime state — generated PNG cache + live token registry
share_cards/
share_tokens.json

# Round 73: Spotify export runtime state — OAuth tokens, never commit
.spotify_tokens.json
.spotify_pending.json
