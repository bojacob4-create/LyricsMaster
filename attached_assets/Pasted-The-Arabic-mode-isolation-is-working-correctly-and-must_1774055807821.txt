The Arabic mode isolation is working correctly and must remain exactly as it is.

What is already confirmed:
- /arabic is isolated correctly
- invalid input stays inside Arabic mode
- /exit restores the normal bot correctly
- the main bot still works correctly after exiting Arabic mode

So the isolation boundary is good.
Do NOT change that part.

The only remaining issue is Arabic-mode search quality.

Observed failures:
- "عمرو دياب - تملي معاك" returned a different Amr Diab song
- "ماجد المهندس - ضايع" was not found
- this means the Arabic-mode resolver/search layers are not strong enough yet

Very important:
DO NOT modify the existing search/retrieval logic used by the main system.
DO NOT touch the current resolver used by:
- /song
- /lyrics
- /analyze
- /translate
- normal dashboard flow
- any existing main-bot search logic

Arabic mode must keep using a completely separate search engine / resolver pipeline.

Now implement the Arabic-mode search plan exactly like this:

==================================================
ARABIC MODE SEARCH PLAN
==================================================

Arabic mode must use its own dedicated layered resolver.

Input inside Arabic mode remains strict:
Artist - Song

No NLP
No natural language
No reuse of the main resolver

--------------------------------------------------
LAYER 0 — PARSE + NORMALIZATION
--------------------------------------------------

Before any search:
- split strict Artist - Song
- keep original text for display
- build normalized search keys separately

Arabic normalization rules:
- remove diacritics
- normalize أ / إ / آ / ٱ → ا
- normalize ى → ي
- normalize ؤ → و
- normalize ئ → ي
- remove standalone ء softly
- remove tatweel
- normalize spaces
- normalize all dash variants to "-"

Examples:
- فؤاد → فواد
- أبي → ابي

Use both:
- original artist/song
- normalized artist/song

Do NOT overwrite the original display text.

--------------------------------------------------
LAYER 1 — PRIMARY ARABIC SEARCH
--------------------------------------------------

Use Arabic exact search first with the original Arabic input.

Primary source:
- YouTube Music search / matching layer

Goal:
- try to resolve the intended Arabic artist + song as cleanly as possible

This layer should favor:
- exact Arabic artist match
- exact Arabic song match
- clean titles
- non-remix / non-live / non-cover / non-karaoke results

--------------------------------------------------
LAYER 2 — NORMALIZED ARABIC SEARCH
--------------------------------------------------

If Layer 1 is weak or ambiguous:
- run the same search using the normalized Arabic artist/song

This is still Arabic search, not English translation.

--------------------------------------------------
LAYER 3 — SECONDARY ARABIC / LYRICS SOURCE
--------------------------------------------------

In parallel with the Arabic YouTube Music path, also try Arabic-mode-only fallback sources.

Use a separate Arabic fallback path for lyrics / metadata:
- lyrics search source(s) dedicated to Arabic mode
- this path must remain isolated from the main bot resolver

If this Arabic fallback produces a stronger and cleaner match earlier than the YouTube Music transliteration path, it may win.

But:
- it must still pass confidence scoring
- it must still pass artist validation

--------------------------------------------------
LAYER 4 — ROMANIZED FALLBACK (INTERNAL ONLY)
--------------------------------------------------

If the Arabic search layers fail or are weak:
- generate limited romanized fallback variants internally
- do NOT expose this to the user
- do NOT ask the user to type English
- do NOT use generic translation
- use transliteration / alias fallback only

Important:
This is NOT literal translation.
This is a hidden fallback for cases like:
- عايض → ayed
- بالموت جا → bel moot ja

Keep this small and controlled.
Do NOT over-generate many variants.

--------------------------------------------------
PARALLEL EXECUTION RULE
--------------------------------------------------

These layers may run in parallel where appropriate:
- Arabic exact
- Arabic normalized
- Arabic fallback source
- romanized fallback (if needed)

But do NOT pick the first returned result blindly.

The winner must be:
- the first HIGH-CONFIDENCE result
not just the fastest result.

--------------------------------------------------
SCORING / RANKING RULE
--------------------------------------------------

Use a clear confidence system for Arabic mode only:

- artist match = 50%
- song match = 40%
- clean title = 10%

Hard reject / strong penalty if title contains:
- remix
- live
- cover
- karaoke
- slowed
- sped up

Arabic mode must also apply strict artist validation.
If the song title matches but the artist does not match strongly enough, reject it.

This is critical for generic song titles and wrong-track problems.

--------------------------------------------------
WINNER SELECTION RULE
--------------------------------------------------

The selected result must:
- exceed a confidence threshold
- clearly beat weaker alternatives
- pass artist validation
- pass clean-title filtering

If no result is strong enough:
return a controlled Arabic-mode failure message only.
Do NOT fall back into the main system.

--------------------------------------------------
LYRICS STEP
--------------------------------------------------

Once Arabic-mode resolver finds the winning official artist/song candidate:
- fetch lyrics using the Arabic-mode lyrics path
- then build the dashboard

If lyrics are not found:
return a controlled Arabic-mode failure message only.

--------------------------------------------------
DASHBOARD RULE
--------------------------------------------------

Keep using the same Song Dashboard output and same buttons.
Do NOT change the dashboard system itself.

Only the Arabic-mode resolver/search sources should change.

==================================================
CRITICAL BOUNDARY
==================================================

You are NOT updating the main bot search logic.
You are building a dedicated Arabic-mode search engine inside /arabic only.

Do NOT:
- refactor the main resolver
- modify main search services
- alter /song behavior
- alter NLP
- alter current routing outside Arabic mode

Arabic mode must remain a self-contained subsystem.

Proceed only inside that boundary.