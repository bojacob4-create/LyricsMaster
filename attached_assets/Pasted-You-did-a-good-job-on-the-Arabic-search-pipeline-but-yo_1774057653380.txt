You did a good job on the Arabic search pipeline, but you did NOT complete the full specification.

You are currently making assumptions instead of verifying behavior. This is NOT acceptable.

You must now COMPLETE the implementation EXACTLY as specified and VERIFY it with real tests before saying “done”.

⸻

🔴 CRITICAL — DO NOT TOUCH MAIN SYSTEM

You are strictly forbidden from:
	•	modifying any existing commands
	•	modifying any existing search logic
	•	importing or reusing any existing search/lyrics services
	•	changing dashboard logic

Arabic mode must remain fully isolated.

⸻

🔴 ISSUE 1 — ISOLATION IS NOT VERIFIED

From previous behavior, Arabic mode was allowing:
	•	/song
	•	/lyrics
	•	natural language

This is WRONG.

You must enforce:

Inside /arabic mode:

✅ ONLY allowed:
	•	Artist - Song
	•	/exit

❌ EVERYTHING else:
	•	must be rejected
	•	must return ONLY the format message
	•	must NOT trigger any other handler

⸻

🔴 HARD REQUIREMENT — TOP-LEVEL INTERCEPTION

At the VERY TOP of the message handler:

If user is in Arabic mode:
	•	intercept immediately
	•	route ONLY to arabic handler
	•	STOP execution completely

No other logic must run.

This must be guaranteed and VERIFIED.

⸻

🔴 ISSUE 2 — YOU DID NOT VERIFY BEHAVIOR

You restarted the bot and said “clean startup”.

That is NOT validation.

You must now run REAL TESTS.

⸻

🧪 REQUIRED TESTS (YOU MUST EXECUTE AND CONFIRM RESULTS)

TEST 1 — Isolation

/arabic
/song Adele - Hello

Expected:
	•	REJECTED
	•	format message returned
	•	Arabic mode still active

⸻

TEST 2 — Natural Language Block

Lyrics hello adele

Expected:
	•	REJECTED
	•	no fallback to main bot

⸻

TEST 3 — Valid Arabic Input

عمرو دياب - تملي معاك

Expected:
	•	correct song (NOT wrong match)

⸻

TEST 4 — Known Failure Case

ماجد المهندس - ضايع

Expected:
	•	FOUND (using new pipeline)

⸻

TEST 5 — Exit Behavior

/exit
/song Adele - Hello

Expected:
	•	main bot works normally again

⸻

🔴 ISSUE 3 — SEARCH PIPELINE MUST FOLLOW SPEC EXACTLY

You MUST confirm:

Phase 1 — YouTube Resolver (PRIMARY)
	•	Arabic search
	•	normalized Arabic search
	•	romanized fallback (last)

Phase 2 — Lyrics
	•	Genius first
	•	lrclib fallback

Matching Rules:
	•	artist ≥ 60%
	•	song ≥ 60%
	•	reject remix/live/cover/etc

⸻

🔴 IMPORTANT

Do NOT:
	•	simplify the pipeline
	•	skip layers
	•	rely only on Genius
	•	merge Arabic mode with main system

⸻

✅ FINAL REQUIREMENT

You are NOT done until:
	1.	All rules are implemented EXACTLY
	2.	All 5 tests above are executed
	3.	You CONFIRM the output of each test clearly

If any test fails:
→ fix it BEFORE responding

⸻

Do not assume. Do not guess. Do not shortcut.

Execute. Verify. Then report.