You must follow these instructions exactly. No shortcuts. No alternative approaches. No modifications outside the defined scope.

⸻

🔒 CRITICAL RULE — DO NOT TOUCH EXISTING SYSTEM

You are strictly forbidden from modifying, refactoring, or interfering with:
	•	Any existing search logic
	•	Any existing APIs or data sources
	•	Any existing command handlers (/song, /lyrics, /analyze, /translate, /random, etc.)
	•	Any scoring system already used in the main bot
	•	Any dashboard logic
	•	Any routing outside Arabic mode

The current system is stable and correct.
You will not reuse, alter, or hook into its search pipeline in any way.

⸻

🧠 ARABIC MODE MUST BE FULLY ISOLATED

Arabic mode is a completely separate environment.

It activates ONLY when:

/arabic

It deactivates ONLY when:

/exit

While inside Arabic mode:

✅ Allowed:
	•	ONLY input in this exact format:

Artist - Song

❌ Forbidden:
	•	Any natural language
	•	Any existing commands (/song, /lyrics, etc.)
	•	Any fallback to main bot logic
	•	Any reuse of main search system

If anything other than Artist - Song is received:
→ Return ONLY the format error message
→ DO NOT trigger any other handler

⸻

🚫 HARD ISOLATION (NON-NEGOTIABLE)

At the VERY TOP of the message handler:

If user is in Arabic mode:
	•	Intercept the message immediately
	•	Route ONLY to Arabic handler
	•	STOP execution completely

No other logic must run after this.

This means:
	•	No regex from main system
	•	No NLP
	•	No command parsing
	•	No fallback

Arabic mode must behave like a separate bot inside the bot

⸻

🔍 ARABIC SEARCH SYSTEM (SEPARATE PIPELINE ONLY)

You must implement a completely independent search pipeline, used ONLY inside Arabic mode.

❗ You are NOT allowed to reuse the existing lyrics/search services.

⸻

🧱 REQUIRED SEARCH LAYERS (IN ORDER)

🔹 Layer 1 — YouTube Resolver (PRIMARY)
	•	Search using Arabic input:

Artist - Song

	•	Extract:
	•	Video title
	•	Channel name
	•	Perform matching:
	•	Artist match (strict)
	•	Song title match (strict)
	•	This layer determines the correct song identity

⸻

🔹 Layer 2 — Arabic Normalization

Before retrying:
	•	Normalize:
	•	All Alef variants → ا
	•	ى → ي
	•	ؤ → و
	•	ئ → ي
	•	remove ء
	•	remove diacritics
	•	trim spaces
	•	normalize dashes

Run YouTube search again with normalized text

⸻

🔹 Layer 3 — Genius (LYRICS SOURCE)
	•	ONLY after song identity is confirmed
	•	Fetch lyrics from Genius
	•	DO NOT use Genius as the primary resolver

⸻

🔹 Layer 4 — Secondary Fallback (lrclib or similar)
	•	Only if Genius fails

⸻

🔹 Layer 5 — Transliteration (LAST RESORT)
	•	Convert Arabic → English
	•	Retry search ONLY if all Arabic layers fail

⸻

🎯 MATCHING RULES (STRICT)
	•	Artist must match ≥ 60%
	•	Song must match ≥ 60%
	•	Reject:
	•	remix
	•	live
	•	cover
	•	slowed
	•	karaoke

If no valid match:
→ Return controlled “not found” message
→ DO NOT fallback to main system

⸻

🎵 DASHBOARD RULE

You MUST reuse the existing Song Dashboard UI.

BUT:
	•	Only pass clean, validated results into it
	•	Do NOT modify dashboard code

⸻

⚠️ IMPORTANT CLARIFICATIONS
	•	You are building a parallel system, not extending the current one
	•	Arabic mode must have its own search brain
	•	The main bot must remain untouched and unaware

⸻

🚫 WHAT YOU MUST NOT DO
	•	Do NOT simplify the system
	•	Do NOT skip YouTube layer
	•	Do NOT rely only on Genius
	•	Do NOT reuse existing services
	•	Do NOT “optimize” by merging systems

⸻

✅ SUCCESS CONDITION

Arabic mode must:
	•	Work independently
	•	Never affect main bot
	•	Never be affected by main bot
	•	Return correct Arabic songs reliably
	•	Exit cleanly with /exit

⸻

If any part of these instructions is unclear, STOP and ask.

Do not proceed with assumptions.